# Copy to `.env.local` (gitignored) and fill in only the providers you have set up.
# Every var below is OPTIONAL — anything missing is silently skipped at runtime.

# --- Firebase (per-excuse copy/share/generate analytics) ---
# Free Spark tier. Firebase Console → ⚙ → Project settings → General → Your apps → Web app config.
# Also enable: Authentication → Sign-in method → Anonymous; and create a Firestore database in production mode.
# Firestore rules (paste in Firestore → Rules tab):
#
#   rules_version = '2';
#   service cloud.firestore {
#     match /databases/{database}/documents {
#       match /events/{doc} {
#         allow create: if request.auth != null;
#         allow read, update, delete: if false;
#       }
#     }
#   }
VITE_FIREBASE_API_KEY=AIzaSyBp6sN3_Y_osiRgqNIwnS3W-51jefgG41g
VITE_FIREBASE_AUTH_DOMAIN=aks1n3d-excusegenerator.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=aks1n3d-excusegenerator
VITE_FIREBASE_APP_ID=1:100284267050:web:fb8b2432c9d2596071a153
VITE_FIREBASE_MEASUREMENT_ID=G-RG8Y5H2JQL

# --- GlitchTip (error tracking, Sentry-compatible) ---
# Free at https://app.glitchtip.com (or self-hosted). Project Settings → Client Keys (DSN).
VITE_GLITCHTIP_DSN=https://0417e17d74674d20b94f60bde79e7d57@errors.aks1n3d.com/8
